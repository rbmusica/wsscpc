<?php
/**
 * WSS Custom Product Configurator Core Functions
 *
 * Funzioni di utilità generale per il plugin.
 *
 * @package WSS_Custom_Product_Configurator
 * @version 1.0.0
 */

// Sicurezza: Impedisce l'accesso diretto al file.
if ( ! defined( 'ABSPATH' ) ) {
	exit;
}

// Attualmente questo file è vuoto, ma è predisposto per future funzioni di utilità.
// Esempio:
// if ( ! function_exists( 'wss_cp_sanitize_slug' ) ) {
//    function wss_cp_sanitize_slug( $string ) {
//        // Logica per sanitizzare uno slug
//        return sanitize_title( $string );
//    }
// }
?>